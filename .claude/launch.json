{
  "version": "0.0.1",
  "configurations": [
    {
      "//": "Attach only, no command. The preview helper has no permission to read files under Desktop on this Mac, so it cannot launch the server itself: start it first with `.venv/bin/python tools/serve.py`, then this entry attaches to it.",
      "name": "schools-publicworks-nyc",
      "url": "http://localhost:8787",
      "port": 8787
    }
  ]
}
